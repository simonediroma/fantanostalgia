# Stato Corrente
> Versionato nel repo — unica memoria persistente tra sessioni web. Aggiornare a fine ogni task.

**Ultima sessione:** 2026-07-14
**Branch attivo:** `claude/manager-email-notifications-ftili7` (imposto dall'harness per questa sessione)
**PR in corso:** nessuna — **sia PR #96 (invio sincrono via BackgroundTasks) sia PR #97 (refactor a coda su DB) sono state mergiate in `main` durante questa sessione.** Storia: PR #96 mergiata dall'utente a metà sessione; il refactor a coda è stato sviluppato *dopo* quel merge sullo stesso branch, quindi riportato a `origin/main` (`git checkout -B claude/manager-email-notifications-ftili7 origin/main`), i 2 commit non ancora mergiati riapplicati con `git cherry-pick` e pushati con `--force-with-lease`; poi, su richiesta esplicita dell'utente ("crea un nuovo branch con le cose che mancano e mandiamolo in prod"), aperta la PR #97 dallo stesso branch (già conteneva esattamente il diff mancante, non serviva un branch con nome diverso) e mergiata subito dopo conferma esplicita dell'utente via `mcp__github__merge_pull_request`. `main` è ora a `8aabdd4`.

**Lavoro di questa sessione — notifiche email ai manager (task ad-hoc, richiesto in chat, non da un prompt in `prompts/`):**
Richiesta utente: email di gioco per 5 eventi — registrazione avvenuta, join a una lega, conclusione risultati di giornata, reminder assegnazione pool nostalgia iniziale, reminder Gran Premio vinto. Deciso con l'utente (2 domande): provider **Resend**, dominio mittente verificato **fantanostalgia.it** (`no-reply@fantanostalgia.it`, non ancora verificato presso Resend — da fare fuori da questa sessione, richiede accesso DNS che l'agente non ha).

**Nessuna migrazione di schema necessaria** — `user.email` esiste già dal task 30 (login coach), quindi niente da chiedere approvazione su `schema.sql`.

Nuovo modulo `backend/api/notifications.py`: `send_email(to, subject, html)` chiama l'API REST di Resend (`https://api.resend.com/emails`) via `httpx` (già dipendenza esistente per i test, nessun nuovo pacchetto). Legge `RESEND_API_KEY`/`EMAIL_FROM` da env; se `RESEND_API_KEY` assente (dev locale, e attualmente anche produzione finché il dominio non è verificato) logga e non invia — no-op esplicito, stesso pattern try/except-e-continua già in uso altrove nel progetto (GCS fallback in `db.py`). Ogni fallimento di invio HTTP viene loggato e mai propagato (`send_email` non solleva mai) — un'email che fallisce non deve mai rompere l'azione principale (lezione "ogni unità di lavoro è indipendente" in `docs/lessons.md`). 5 funzioni `notify_*` con layout HTML inline-style condiviso (`_layout()`, i client email non supportano CSS esterno) + helper `league_manager_emails(conn, league_id)` che ritorna solo i manager con `user_id` collegato (non tutti i manager hanno necessariamente registrato un account).

**5 punti di aggancio, tutti endpoint esistenti, nessun nuovo endpoint creato:**
- Registrazione → `POST /auth/register` (`auth.py`) — email al neo-registrato dopo il commit, con il nome della lega dell'invito appena consumato.
- Join a una lega → `POST /auth/user/join` (`auth.py`) — stesso pattern per un utente già registrato che consuma un secondo invito.
- Conclusione giornata → `calculate_matchday_scores` (`matchday.py`) **e** `process_pending_matchdays` (cron settimanale via GitHub Actions, stesso endpoint bulk) — entrambi chiamano un helper condiviso `_notify_matchday_conclusion()` per evitare duplicazione, un'email per manager collegato della lega.
- Reminder pool iniziale → `assign_pools` (`mapping.py`) — un'email per ogni manager collegato della lega, dopo l'assegnazione.
- Reminder Gran Premio vinto → `resolve` (`granpremio.py`) — solo se il manager vincitore ha un `user_id` collegato (altrimenti nessuna email, verificato con un test dedicato), include il nome dello storico vinto.

Tutti gli endpoint toccati ora accettano `request: Request` (per `request.base_url`, stesso pattern già usato da `create_invite` in `league.py`) e `background_tasks: BackgroundTasks` (invio non bloccante, accodato dopo il commit della transazione DB).

**Test:** nuovo file `backend/tests/test_notifications.py`, 8 test — uno per ciascuno dei 5 eventi (mock di `notifications.send_email` via monkeypatch, verifica destinatario/subject/contenuto), un test esplicito che il vincitore Gran Premio senza `user_id` non riceve email, 2 test su `send_email` stesso (no-op senza API key, non solleva eccezioni se la chiamata HTTP fallisce). Suite completa: 203 passed (195 + 8), stessi 3 fallimenti pre-esistenti in `test_scoring.py` + 3 errori pre-esistenti in `test_fbref_scraper.py` (non correlati, documentati da sessioni precedenti — ambiente di questa sessione non ha `pytest-mock` installato di default, richiesto `pip install -r backend/requirements.txt` a mano su `python3.11` perché `fastapi` non era installato in nessun python del container).

Non verificato: invio email reale (nessuna `RESEND_API_KEY` disponibile in questa sessione) — verificato solo end-to-end via mock nei test automatici e via un server locale con la coda (vedi sotto), il payload HTTP verso l'API Resend non è mai stato osservato contro il servizio reale.

**Follow-up nella stessa sessione — configurazione DNS Resend + refactor a coda su DB:**

Simone ha verificato il dominio `fantanostalgia.it` su Resend (record DKIM/MX/SPF su `send.fantanostalgia.it` e `resend._domainkey.fantanostalgia.it`, propagati via Aruba — assistito con un documento pronto da girare al supporto Aruba, poi risolto autonomamente). In parallelo, discutendo l'architettura dell'invio email (sincrono via `BackgroundTasks` introdotto nella prima parte della sessione), è emerso un problema reale per questo deploy: **Cloud Run gira senza `--no-cpu-throttling`** (verificato in `cloudbuild.yaml` — nessun flag di CPU always-allocated), quindi un `BackgroundTask` che lavora dopo la risposta HTTP rischia di essere affamato di CPU e non completarsi mai in produzione (funziona sempre in test/locale, dove la CPU non è mai limitata — falso senso di sicurezza).

Simone ha proposto una coda su DB (tipo/template + parametri di sostituzione) con scodamento schedulato separato. Deciso con l'utente in plan mode (2 domande): **trigger dello scodamento = GitHub Actions cron** (non Cloud Scheduler né un vero Cloud Run Job — verificato che nessuno dei due esiste realmente nel progetto, solo menzionati come aspirazionali in `docs/architecture.md`; l'unico cron reale è `weekly-draw.yml`), **latenza accettabile = 15-30 minuti** → cron ogni 10 minuti.

**Refactor completo di `backend/api/notifications.py`:** i 5 `notify_*` sono diventati funzioni pure di rendering `_render_*(params, base_url) -> (subject, html)` registrate in un dizionario `TEMPLATES`; nuove funzioni `enqueue_email(conn, template, to, params)` (INSERT nella stessa transazione del trigger point, nessun round-trip GCS aggiuntivo) e `process_email_queue(conn, base_url, batch_size, max_attempts)` (FIFO, un solo `get_db()` per l'intero batch — un solo upload GCS, non uno per email). `send_email` non inghiotte più le eccezioni (prima le catturava e basta, per il fire-and-forget) — ora solleva, perché il processore di coda deve sapere se il singolo invio è fallito per decidere retry/dead-letter (`MAX_EMAIL_ATTEMPTS=5`, poi la riga diventa `status='failed'` e non viene più ritoccata). `DEFAULT_BATCH_SIZE=20` (non 50 come proposto inizialmente da un sub-agent di design): con l'intero batch in un'unica transazione/commit, un crash a metà (timeout Cloud Run, OOM, deploy concorrente) perderebbe la transazione ma non i `send_email` già eseguiti su Resend — rischio di email duplicate al giro successivo. Con batch=20 e timeout httpx 10s, il caso peggiore (tutti in timeout) resta sotto i 300s di timeout default di Cloud Run; con 50 li avrebbe superati, quasi garantendo il crash a metà. Rischio di duplicato residuo accettato esplicitamente (email non critiche) piuttosto che pagare un upload GCS per singola email.

Nuova tabella `email_queue` in `backend/api/db.py::init_db()` (stessa convenzione di `gran_premio`/`admin_elevation_request` — mai in `database/schema.sql`): `id, template, to_email, params (JSON), status (pending/sent/failed), attempts, last_error, created_at, sent_at` + indice su `status`. Tutti e 5 i trigger point (`auth.py::register/user_join_league`, `matchday.py::calculate_matchday_scores/process_pending_matchdays` via helper condiviso rinominato `_enqueue_matchday_conclusion`, `mapping.py::assign_pools`, `granpremio.py::resolve`) hanno perso i parametri `Request`/`BackgroundTasks` (non più necessari — `base_url` viene calcolato una sola volta dal processore di coda sul proprio dominio pubblico, identico indipendentemente da chi ha originato l'email) e ora chiamano `enqueue_email(conn, ...)` dentro il blocco `with get_db()` già esistente.

Nuovo endpoint `POST /admin/process-email-queue` in `matchday.py` (stessa dipendenza auth `get_current_admin_or_bearer` di `/admin/process-pending`, stesso Bearer token). Nuovo workflow `.github/workflows/process-email-queue.yml`, cron `*/10 * * * *`, identico nel pattern a `weekly-draw.yml` (stessi secret `API_SECRET_KEY`/`API_BASE_URL`, nessun nuovo secret).

**Test:** `backend/tests/test_notifications.py` riscritto quasi interamente (11 test) — i 6 trigger ora asseriscono che una riga sia stata accodata (non più invio sincrono catturato via monkeypatch), + invio end-to-end via `/admin/process-email-queue`, + retry-poi-dead-letter dopo 5 tentativi, + contratto aggiornato di `send_email` (solleva invece di inghiottire). Scoperta in corso d'opera: la fixture `client` è **session-scoped** e condivisa con l'intera suite (es. `test_elevation.py` registra 5 coach), quindi le prime versioni dei test che contavano righe per `template` fallivano quando eseguiti nella suite completa (contaminazione cross-file) — corretto filtrando per `to_email` (univoco per test) invece che per `template`, e aggiungendo un helper `_drain_queue()` per i 2 test sul processore che devono partire da coda vuota. Suite completa: 206 passed (203 + 3 netti, alcuni vecchi test rimossi/sostituiti), stessi 3 fallimenti pre-esistenti in `test_scoring.py` + 3 errori pre-esistenti in `test_fbref_scraper.py` (non correlati).

Verificato end-to-end anche contro un server `uvicorn` locale reale (non solo i test fixture): registrazione → riga `pending` in `email_queue` → `POST /admin/process-email-queue` → riga passa a `sent`.

## Prossima sessione — inizia da qui (per questo task)

**PR #97 mergiata in `main`.** Generato e comunicato all'utente un valore sicuro per `SECRET_KEY` (`secrets.token_hex(32)`), da impostare a mano su Cloud Run insieme ad `ADMIN_PASSWORD` vera e `RESEND_API_KEY` — **non ancora confermato dall'utente di averlo effettivamente incollato/salvato**, solo generato e consigliato.

**Scoperta importante su `.github/workflows/deploy.yml`:** verificato via `mcp__github__actions_list`/`get_job_logs` che questo workflow **gira automaticamente ad ogni push su `main`** (confermate 88 esecuzioni) **ma fallisce sempre** all'ultimo step (`gcloud run deploy`), inclusa l'esecuzione innescata dal merge di PR #97: `PERMISSION_DENIED: iam.serviceaccounts.actAs` sul service account di compute — il service account usato da GitHub Actions (dentro il secret `GCP_SA_KEY`) non ha il ruolo `roles/iam.serviceAccountUser` necessario. **Non ha mai deployato nulla con successo.** L'utente ha confermato che il deploy reale avviene tramite un trigger Cloud Build nativo su GCP, indipendente da questo workflow — quindi `deploy.yml` è di fatto morto/inutilizzato (non ancora deciso con l'utente se rimuoverlo). Conseguenza pratica: i secret GitHub `ADMIN_USERNAME`/`ADMIN_PASSWORD`/`GCP_SA_KEY`/`GCS_BUCKET`/`GCP_PROJECT` (usati solo da `deploy.yml`) **non hanno oggi alcun effetto sul servizio reale** — vanno impostati manualmente su Cloud Run, non bastano su GitHub. Diverso il discorso per `API_SECRET_KEY`/`API_BASE_URL`: servono davvero, sono usati da `weekly-draw.yml`/`process-email-queue.yml` (semplici `curl` verso il servizio già in esecuzione, indipendenti dal deploy) — `API_SECRET_KEY` deve combaciare esattamente col valore di `SECRET_KEY` impostato su Cloud Run.

**Chiarito con l'utente il comportamento email per manager multi-lega:** un solo account/email per persona (un utente può accettare più inviti collegando lo stesso `user_id` a più righe `manager` in leghe diverse via `/auth/user/join`), ma `league_manager_emails()` è scoped per singola lega — quindi un manager attivo in più leghe riceve **più email indipendenti** (una per lega per evento, non un digest cumulativo). Comportamento corretto by design, non un bug, nessuna modifica necessaria.

**Follow-up nella stessa sessione — reset password admin-triggered:**

Richiesta utente: un manager che perde la password non ha modo di recuperarla (nessun flusso self-service, nessuna azione admin, nessuno script). In plan mode: prima proposta un flusso a token (link email che il coach consuma per impostare da solo la nuova password, con tabella `password_reset_token` dedicata, scadenza 1h, single-use) — **respinta esplicitamente dall'utente**: "la password deve essere selezionata dall'admin non generata random la sceglie l'admin e la comunicazione arriva per email automatica e per comunicazione diretta admin allenatore". Ridisegnato molto più semplice: l'admin sceglie direttamente la nuova password, il sistema la applica subito, il coach la riceve via email automatica (più eventuale comunicazione diretta admin→allenatore fuori app, nessun codice necessario per quel secondo canale). Nessuna nuova tabella, nessun nuovo endpoint pubblico, nessuna nuova pagina coach.

Nuovo endpoint `POST /admin/league/{league_id}/managers/{manager_id}/reset-password` in `league.py` (stessa auth `get_current_admin` di `create_invite`, stesso file): verifica manager esistente e `user_id` collegato (400 altrimenti — non si può resettare la password di un account che non esiste), aggiorna `user.password_hash` con `_pwd_ctx.hash(new_password)` (importato da `auth.py`, prima simbolo privato mai importato da un altro router), accoda un'email `password_reset` con la password in chiaro nei `params`. Nessuna validazione di lunghezza minima sulla password, coerente con `register()`/`user_login()` che non ne impongono nessuna.

**Mitigazione di sicurezza esplicita:** la password in chiaro finirebbe altrimenti a vivere per sempre in `email_queue.params` (le righe non vengono mai cancellate, solo marcate `sent`). In `process_email_queue`, dopo un invio riuscito per il template `password_reset`, i `params` vengono sovrascritti con `{"redacted": true}` invece di lasciare la password in chiaro nel DB indefinitamente — la riga resta come traccia dell'evento (template/to_email/sent_at), solo il contenuto sensibile sparisce. Comportamento invariato per gli altri 4 template (nessun dato sensibile nei loro params).

UI: nuovo bottone "Resetta password" nella tabella "Stato allenatori" (stessa riga di "Genera link" in `frontend/admin/index.html`), disabilitato se il manager non ha un account collegato. Apre un modal con un campo password (`DS.Modal()` + `DS.Input(type='password')`, stesso pattern del modal "Link invito" già esistente) — il modal stesso è la conferma, nessun `DS.confirmDialog` aggiuntivo sopra (sarebbe una doppia conferma ridondante).

**Test:** nuovo file `backend/tests/test_password_reset.py`, 6 test — cambio password effettivo (login con la vecchia fallisce, con la nuova riesce), email accodata con i parametri corretti, redazione dei params dopo l'invio via `/admin/process-email-queue`, manager senza account collegato (400), manager/lega inesistente (404), chiamata non autenticata (401). Suite completa: 212 passed (206 + 6), stessi 3 fallimenti pre-esistenti in `test_scoring.py` + 3 errori pre-esistenti in `test_fbref_scraper.py` (non correlati). Verificato anche end-to-end contro un server `uvicorn` locale reale: reset → login vecchia password 401, nuova password 200 → riga pending in coda con la password in chiaro → `/admin/process-email-queue` → riga `sent` con `params` redatto.

Committato (`4ac8e0c`) e pushato sullo stesso branch (verificato prima con `git merge-base --is-ancestor origin/main HEAD` che fosse un fast-forward pulito da `main`, nessun cherry-pick necessario stavolta). **Nessuna PR aperta per questo commit** — da aprire solo su richiesta esplicita dell'utente, come da convenzione di questa sessione.

Prossimi passi possibili: (1) aprire una PR per questo commit se l'utente lo richiede; (2) verificare con l'utente se ha effettivamente salvato le variabili su Cloud Run; (3) decidere se sistemare il permesso IAM di `deploy.yml` o eliminare il file; (4) proseguire con l'epica 4 (design system) o con le spike bloccate 32-35, secondo le note sotto.

---

**Lavoro di questa sessione — tab "Giornate" in Admin (vista aggiuntiva rispetto al wizard):**
Richiesta utente: nella sezione admin di lega, un tab "Giornate" con (1) upload Excel formazioni, (2) elenco di tutte le giornate caricate, (3) sorteggio giornata storica eseguibile inline da singola riga, (4) quick-button calcolo punteggi da riga, (5) click su riga → dettaglio giornata con risultati/statistiche + gestione Gran Premi (creazione e risoluzione). Esplicitamente una vista aggiuntiva: lo Step 4 del wizard di setup (`ws-4` in `frontend/admin/index.html`) non è stato toccato, resta l'unico posto per il flusso guidato.

Backend: nuovo endpoint `GET /admin/league/{id}/matchdays` in `backend/api/routers/matchday.py` — aggrega `lineup` (giornate caricate) LEFT JOIN `matchday_draw` (stato sorteggio) + subquery su `matchday_score` (conteggio punteggi calcolati), un solo round-trip per popolare l'elenco con lo stato completo di ogni giornata (Caricata/Sorteggiata/Calcolata). Stessa auth delle altre route admin di `matchday.py` (`get_current_admin_or_bearer`).

Frontend: 2 nuove pagine SPA in `frontend/admin/index.html` — `pg-giornate` (elenco, con upload formazioni proprio + `DS.Table` con azione inline Sorteggia/Calcola per riga via `onRowClick` + bottoni con `e.stopPropagation()`) e `pg-giornata-detail` (stato sorteggio, tabella punteggi, Gran Premi scoped alla giornata). Bottone "📅 Giornate" aggiunto nell'header di `pg-manage` (accanto a "← Leghe") per entrare nella nuova vista: `openGiornateBtn`. Per i risultati/statistiche della giornata nel dettaglio, **riuso diretto** dell'endpoint JSON già esistente `GET /api/lega/{id}/calendario/{matchday}` (creato per il tab pubblico "Calendario" di `classifica.html`, PR #91) — stesso JSON, stesse classi CSS `.cal-*` (portate in `frontend/admin/css/admin.css`, sezione dedicata) e stessa logica di rendering (icone bonus/malus, nome alter ego prioritario) riscritta in JS con prefisso `gt` (`gtRenderCalMatches`, `gtPlayerRows`, `gtBonusIcons`, ecc.) per evitare collisioni — nessuna duplicazione di logica server-side, solo porting del rendering client-side da Jinja2 inline-script a SPA. Se la giornata non ha `h2h_match` (formato "flat" di upload formazioni, senza pairing squadre) il pannello risultati non viene mostrato, solo la tabella punteggi — comportamento corretto, non un bug (verificato).

Nuovi test: 6 in `backend/tests/test_matchday.py` (elenco vuoto, giornata solo caricata, giornata sorteggiata+calcolata, ordinamento DESC, lega inesistente 404, auth richiesta) — 196 passed (190 + 6), stessi 3 fallimenti pre-esistenti in `test_scoring.py` + 3 errori pre-esistenti in `test_fbref_scraper.py` (non correlati, documentati da sessioni precedenti, confermato di nuovo che non sono causati da questo cambiamento).

Verificato end-to-end con Playwright headless contro un server locale (non i test fixture, un'istanza `uvicorn` reale con DB SQLite locale) creando lega/manager/listone/formazioni via chiamate HTTP dirette: upload formazioni G1+G2 → elenco mostra entrambe con stato corretto → click "Sorteggia" su riga G2 aggiorna la riga inline (badge "SORTEGGIATA", bottone cambia in "CALCOLA PUNTEGGI") senza navigare via dal tab → click sulla riga G1 (già sorteggiata e calcolata) apre il dettaglio → tabella punteggi corretta, pannello Gran Premi funzionante (creazione/lista, vuoto perché nessun pool nostalgia assegnato in questo test manuale). Zero errori JS reali (solo `ERR_CONNECTION_RESET` su Google Fonts dal sandbox di rete e un 401 atteso su `/auth/me` pre-login, stesso rumore documentato in ogni sessione precedente).

Non verificato: comportamento su mobile/viewport stretto per le 2 nuove pagine (non modificate le regole responsive esistenti — riusano `.page`/`.ds-panel`/`.ds-table` già responsive dalle sessioni 25-28, nessuna ragione per attendersi regressioni ma non testato esplicitamente questa volta per limiti di tempo).

**Follow-up nella stessa sessione — ricarica formazioni dal dettaglio giornata:**
Richiesta utente successiva: nel dettaglio di una singola giornata (`pg-giornata-detail`), poter ricaricare il file Excel delle formazioni senza tornare all'elenco. Aggiunto un pannello collassabile (`<details id="gtReloadDetails">`, pattern coerente con "Dati storici (avanzato)" della dashboard) con dropzone + bottone dedicati (`gtDetailDropZoneSlot`/`gtDetailUploadBtn`), che riusa l'endpoint di upload già esistente `POST /admin/league/{id}/lineups/{matchday}` (idempotente, nessun nuovo endpoint) passando `detailMatchday` fisso invece di un campo numerico libero.

Vincolo aggiunto dall'utente a metà implementazione: **il ricaricamento va bloccato se per quella giornata esiste già un Gran Premio risolto** (il vincitore ha già ricevuto il premio in base ai dati correnti — ricaricare formazioni/voti dopo la risoluzione creerebbe un disallineamento tra l'esito assegnato e i dati sottostanti). Applicato **lato backend** in `upload_lineups` (`backend/api/routers/lineups.py`) con una query su `gran_premio WHERE status = 'resolved'` prima di processare il file — 400 esplicito se presente. Scelta deliberata di mettere il check nell'endpoint condiviso (non solo nel nuovo pannello frontend) perché lo stesso endpoint è usato anche dallo Step 4 del wizard e dall'upload nell'elenco Giornate: il vincolo deve valere per qualunque re-upload dello stesso `matchday`, non solo per questa nuova UI. Frontend: `gtUpdateReloadState()` disabilita il pannello (bottone disabled + testo "🔒 non disponibile") controllando lo stato dei Gran Premi già caricato da `loadGtGpList()` (nessuna chiamata API aggiuntiva) — difesa in profondità, il backend è comunque la fonte di verità.

Nuovi test: 4 in `backend/tests/test_lineups.py` (upload bloccato con GP risolto sulla stessa giornata, upload consentito su un'altra giornata della stessa lega, upload consentito con GP ancora attivo/non risolto) — 194 passed totali, stessi fallimenti pre-esistenti non correlati.

Verificato end-to-end con Playwright headless: creata una lega con una giornata sorteggiata+calcolata+Gran Premio risolto (inserito direttamente via query SQL, bypassando il flusso di risoluzione completo — non era necessario testare `resolve_gran_premio` stesso, già coperto da `test_granpremio.py`) e una seconda giornata senza Gran Premio — il pannello di ricarica risulta bloccato (bottone disabled, testo con 🔒) sulla prima e utilizzabile sulla seconda; upload reale eseguito con successo sulla seconda tramite file chooser Playwright, messaggio di conferma corretto. Nessun errore console/JS.

**PR #94** (tab "Giornate" in Admin) aperta dall'utente durante la sessione e **mergiata in `main`** poco dopo (commit `bc486ce`, merge `3269fd4`) — non più il riferimento per i push successivi.

**Fix nella stessa sessione — asset JS/CSS delle SPA serviti stale dal browser:**
Segnalato dall'utente con screenshot: `ReferenceError: apiListMatchdays is not defined` sul tab Giornate, nonostante la funzione fosse presente nel sorgente (verificato leggendo il file e ri-servendolo via `curl` da un `uvicorn` locale — il codice sul backend era corretto). Causa più probabile: `frontend/admin/js/*`, `frontend/coach/js/*` e `frontend/shared/*` sono montati come `StaticFiles` senza alcun header di cache esplicito e senza cache-busting nell'URL (stesso path ad ogni deploy) — un browser può continuare a servire una versione in cache dopo un aggiornamento del codice. Pushato inizialmente sul branch `claude/league-admin-matchday-tab-r03hf2` senza accorgersi che PR #94 era nel frattempo già stata mergiata dall'utente — **corretto**: l'utente ha segnalato "manda su nuovo branch, la 94 era già su main", quindi il branch è stato riportato a `origin/main` (`git checkout -B claude/league-admin-matchday-tab-r03hf2 origin/main`) e i 2 commit non ancora mergiati (fix cache + relativo aggiornamento memoria) sono stati riapplicati con `git cherry-pick` sopra la nuova base, poi pushati con `--force-with-lease`. Nessuna PR aperta per questi 2 commit al momento — da aprire su richiesta esplicita dell'utente.

Aggiunto un middleware FastAPI globale in `backend/api/main.py` (`no_cache_static_assets`) che imposta `Cache-Control: no-cache` sulle risposte per i path `/admin/js/`, `/admin/css/`, `/coach/js/`, `/coach/css/`, `/shared/` (forza la revalidazione ad ogni richiesta invece di disabilitare la cache — resta comunque efficiente via ETag/Last-Modified, solo niente più contenuto stantio senza una richiesta al server). `/static/*` (pagine pubbliche SSR) non è toccato, deliberatamente escluso dal prefix-match. Verificato con `curl -I` contro un server locale: header presente su `/admin/js/matchday.js` e `/shared/design-system.js`, assente su `/static/style.css`. Nuovi test: 3 in `backend/tests/test_static_headers.py` (nuovo file) — 195 passed totali, stessi fallimenti pre-esistenti non correlati.

Non risolto con certezza: la causa esatta lato utente (browser cache heuristica vs. altro) non è stata osservabile direttamente da questa sessione — il fix è una mitigazione strutturale corretta a prescindere dal meccanismo esatto, ma se il problema si ripresenta dopo il redeploy con questo fix, va indagato più a fondo (es. `docker-compose.yml`: il servizio `backend` monta `./backend:/app` senza montare anche `./frontend`, quindi se l'utente esegue `docker-compose up` senza rebuild dell'immagine, `frontend/` non è affatto raggiungibile nel container — notato ma non affrontato in questa sessione perché non spiega il sintomo osservato, che mostrava l'admin renderizzato correttamente tranne una funzione).

**Convenzione branch:** `task/NN-nome-breve` — un branch per task, PR verso `main`.

---

## Prossima sessione — inizia da qui

Aprire la PR per `claude/league-admin-matchday-tab-r03hf2` (tab "Giornate" in Admin) se l'utente lo richiede — per ora solo pushato. Se richiesto, verificare che il deploy Cloud Run mostri la homepage correttamente stilizzata dopo PR #90 (mai testato contro un deploy reale).

Poi: dobbiamo implementare il design system e alcune dinamiche di gioco con l'epica 4.

### Scraper disponibili (scegli uno)

**Opzione A — calcio-seriea.net (consigliata, no Cloudflare):**
```
pip install -r backend/requirements-scraper.txt
python -m backend.scrapers.calcioseriea --season 2016-2017 --export-csv out.csv
```

**Opzione B — understat (stagioni >= 2014/15, eseguire in locale):**
```
python -m backend.scrapers.understat --season 2015-2016 --export-csv out.csv
```

**Opzione C — fbref con Playwright (qualsiasi stagione, richiede browser locale):**
```
python -m backend.scrapers.fbref_pw --season 2000-2001 --export-csv out.csv
```

**Opzione D — Web Scraper Chrome manuale (fbref, vedi `docs/webscraper/`):**
Scarica sitemap da `GET /webscraper/seasons.html`, importa con Web Scraper Chrome, converti con `convert_webscraper.py`.

### Dopo lo scraping
```
POST /admin/historic/import   # importa CSV con i dati storici
```

### Per modificare pesi senza riscrappare
```
python -m backend.engine.recalculate --dump-weights pesi.json
# edita pesi.json
python -m backend.engine.recalculate --season 2016-2017 --weights-file pesi.json
```

### Coach API attiva
Gli allenatori possono fare login su `/coach/login.html` e vedere la propria rosa nostalgia. L'associazione manuale storico→attuale è disponibile da admin.

Ogni task ha un prompt dedicato in `prompts/`.
Prima di iniziare qualsiasi task: leggi il prompt corrispondente + `docs/architecture.md`.

---

## Task list

### Seed & Setup
- [x] **00** — Seed dati POC → `prompts/00-seed-poc.md` (branch: `task/00-seed-poc`)

### Epica 1 — Backend
- [x] **01** — Bootstrap (DB + Auth + Gestione Lega) → `prompts/backend/01-bootstrap.md` (branch: `task/01-bootstrap`)
- [x] **02** — Import listone Excel → `prompts/backend/02-import-listone.md` (branch: `claude/nice-albattani-4197op`)
- [x] **03** — Algoritmo mapping alter ego → `prompts/backend/03-mapping-alter-ego.md` (branch: `claude/gallant-archimedes-lwbn3i`)
- [x] **04** — Apertura buste → `prompts/backend/04-apertura-buste.md` (branch: `claude/kind-volta-oqq3rf`)
- [x] **05** — Import formazioni Excel → `prompts/backend/05-import-formazioni.md` (branch: `claude/tender-allen-uyaspn`, formato POC)
- [x] **06** — Sorteggio giornata storica → `prompts/backend/06-sorteggio-giornata.md` (branch: `claude/prossimo-task-gztmd8`)
- [x] **07** — Calcolo punteggi → `prompts/backend/07-calcolo-punteggi.md` (branch: `claude/prlssimo-task-fzkcwv`)
- [x] **08** — API classifica pubblica → `prompts/backend/08-api-classifica.md` (branch: `claude/memory-file-review-fecztn`)

### Epica 2 — Frontend
- [x] **09** — Layout base + navigazione → `prompts/frontend/09-layout-base.md` (branch: `claude/task-09-nsvx6j`)
- [x] **10** — Admin pages (setup + listone + mapping + formazioni) → `prompts/frontend/10-admin-pages.md` (branch: `claude/next-steps-ocl3qu`)
- [x] **11** — Pagine pubbliche SSR Jinja2 → `prompts/frontend/11-pagine-pubbliche.md` (branch: `claude/prossimo-task-lo5jxx`)

### Epica 3 — Utilities & DevOps
- [x] **12** — DevOps (Docker + GH Actions + Cloud Run) → `prompts/utilities/12-devops.md` (branch: `claude/task-12-pl3qh8`)
- [ ] **13** — Scraper fantagiaveno.it → `prompts/utilities/13-scraper-fantagiaveno.md` *(deprioritizzato: sostituito da approccio fbref+algoritmo)*
- [x] **14** — Scraper fbref + motore sintetico → branch `claude/data-scraping-approach-jksb5l` (PR #31)
- [x] **15** — Adattamento formato Excel reale → (branch: `claude/gifted-faraday-brf0xr`)
- [x] **16** — Redesign estetica 8-bit Sensible Soccer → `prompts/utilities/16-redesign-8bit.md` (branch: `claude/prossimo-task-hyg0j4`)

### Extra (post-epica)
- [x] **17** — Scraper fbref con Playwright (bypass Cloudflare) → (branch: `task/fbref-playwright`, PR #33)
- [x] **18** — Web Scraper Chrome sitemap + `convert_webscraper.py` → (branch: `task/fbref-webscraper-sitemap`, PR #34)
- [x] **19** — Coach API: auth allenatore, pool nostalgia, associazione manuale storico→attuale → (branch: `claude/nostalgia-player-association-u5semr`, PR #35)
- [x] **20** — Fix CI/CD: switch su Artifact Registry per Cloud Run deploy → (branch: `task/fix-artifact-registry`, PR #36)
- [x] **21** — Guida step-by-step seasons.html per multi-season scraping → (branch: `task/seasons-html-links`, PR #38)
- [x] **22** — Scraper calcio-seriea.net con ruoli reali (fonte alternativa, no Cloudflare) → (branch: `claude/upbeat-fermat-98un5f`, PR #39)
- [x] **23** — Gran Premi di giornata: il presidente attiva max 2 GP/giornata (criterio: best_score/worst_defense/best_player/worst_player) con uno storico libero in palio; alla risoluzione il vincitore riceve lo storico come slot extra nel pool nostalgia e riapre l'associazione (coach) → (branch: `claude/project-analysis-features-4c2s9i`). Backend: `backend/engine/granpremio.py`, router `backend/api/routers/granpremio.py`, tabella `gran_premio` in `db.py`, helper `compute_player_breakdown` in `scoring.py`. Frontend: pannello Step 4 admin + avviso coach in `rosa.html` + sezione "Gran Premi" nella pagina pubblica `giornata.html` (con query in `views.py`). Fix collaterale: rimossa colonna inesistente `hr.penalties_saved` dalla query giornata in `views.py` (causava 500 sulla pagina). Test: `backend/tests/test_granpremio.py`.

### Epica 4 — Design System Restyling
 
- [x] **24** — Libreria design system vanilla JS (porting da `_ds_bundle.js` React, 13 componenti) → `prompts/frontend/24-design-system-vanilla-js.md` (branch: `claude/session-planning-ragfwp`). Creati `frontend/shared/design-system.js` (namespace `window.FantaNostalgiaDS`, 13 factory function: Badge, Button, Panel, Table, EmptyState, HelpBox, Message, ProgressBar, DropZone, Input, WizardSteps, Tabs, Modal), `frontend/shared/design-system.css`, `frontend/shared/tokens/{colors,spacing,typography,effects,fonts}.css` (single source of truth, non ancora adottati dai 3 CSS esistenti — verrà fatto nei task 25-27), `frontend/shared/demo.html` (verifica visiva manuale). Verificato con Playwright headless: zero errori JS, zero riferimenti a React/ReactDOM, tutte le varianti/stati conformi a `_ds_bundle.js` e `project-spec.md` sez. 11.
- [x] **25** — Restyle pagine pubbliche SSR + sezione Hall of Fame (branch: `claude/prossimo-task-1wzl48`, dipende da 24). Nessun prompt scritto in `prompts/` per questo task (gap rispetto agli altri task, segnalato e chiarito con l'utente a inizio sessione con 2 domande: criterio Hall of Fame = top 10 cross-stagione, soglia >=5 giornate min.; migrazione token = sì, subito). Modifiche: `backend/api/main.py` monta `/shared` → `frontend/shared/` come static; `base.html` carica `/shared/design-system.css` prima di `/static/style.css`; `backend/static/style.css` ridotto da 367 a ~180 righe — rimossi `:root`, reset globale, scanline CRT, regole bare `button`/`table`/`.tabs`/`.tab-btn`/`.role-badge`/`.empty`/`.badge`/`.btn-link` (ora tutti provenienti da `frontend/shared/design-system.css`/tokens); le 5 pagine pubbliche (`home.html`, `classifica.html`, `giornata.html`, `statistiche.html`, `mapping.html`) usano ora le classi `ds-*` (ds-button, ds-table, ds-tabs__nav/__tab/__panel, ds-badge, ds-empty-state, ds-message) mantenendo identica la logica JS esistente (tab switching riscritto per usare `hidden` + `aria-selected` invece di classi `.active`). Fix collaterale nel design system condiviso: aggiunta regola `.ds-message[hidden] { display: none; }` in `frontend/shared/design-system.css` — senza, l'attributo `hidden` nativo veniva sovrascritto da `display:flex` del componente (bug latente scoperto verificando la home con Playwright). Hall of Fame: nuova query cross-stagione in `views.py::home()` (`player_historic` JOIN `historic_rating`, `HAVING n >= 5 LIMIT 10`), sezione mostrata solo se `hall_of_fame` non vuoto. Test: `backend/tests/test_home_hall_of_fame.py` (2 test: soglia minima giornate, presenza sezione). Verificato visivamente con Playwright headless (screenshot + interazioni tab/login) su tutte le 5 pagine con dati POC (`database/seed_poc.py`); suite completa 172 passed, 3 fallimenti pre-esistenti non correlati in `test_scoring.py` (confermato con `git stash` su main pulito). **Follow-up 2026-07-09** (branch `claude/task-25-responsive-followup`): dopo che l'utente ha aggiunto `prompts/frontend/25-restyle-pagine-pubbliche.md` a posteriori (commit diretto su `main`), verificati gli acceptance criteria e trovati 3 gap non coperti dalla PR #80: mancava il componente Panel per le card (`league-card`, `step`, `auth-card` in `home.html` ora usano `ds-panel`), mancava la navigazione mobile con hamburger (aggiunto `#navToggle`/`#siteNav` in `base.html` + CSS `.nav-toggle`/`nav.open` sotto 640px), le tabelle dense di classifica/giornata usavano solo `overflow-x:auto` silenzioso invece del pattern stack-a-card richiesto esplicitamente dal prompt (aggiunta classe `.ds-table-stack` con `data-label` sui `<td>`, attiva sotto 480px). Verificato con Playwright a 3 viewport (1200/768/375px), zero errori console, hamburger e stacking tabelle testati via interazione JS.
- [x] **26** — Restyle Admin SPA (wizard 4 step, incl. invito coach esistente) → nessun prompt scritto in `prompts/` (gap segnalato e chiarito con l'utente a inizio sessione: scelto restyle CSS-only, come task 25, invece di componentizzazione JS completa con le factory function di `design-system.js`). Branch: `claude/prossimo-task-vwa0s9` (non `task/26-restyle-admin-spa` come da convenzione — il branch è imposto dall'harness per questa sessione). Modifiche: `frontend/admin/index.html` carica `/shared/design-system.css` prima di `css/admin.css`; tutte le classi custom (`.panel`→`ds-panel ds-panel--default`+`ds-panel__title` su h4, `button`/`.secondary`/`.danger`→`ds-button`+varianti, `.table-wrap`+`table`→`ds-table-wrap`+`ds-table`, `.msg`→`ds-message`, `.empty`→`ds-empty-state`+`ds-empty-state__message`, `.badge`→`ds-badge`, `.drop-zone`→`ds-dropzone`, `.modal-overlay`/`.modal-box`→`ds-modal-overlay`/`ds-modal`, `.steps`/`.step`→`ds-wizard-steps`/`ds-wizard-steps__step`) sostituite sia nel markup statico sia nei template string generati da JS (dashboard leghe, tabella manager, tabella assegnazione giocatori con badge ruolo cyan, stato allenatori, riepiloghi listone/mapping/punteggi, Gran Premi, mapping completo buste). Refactor minimo della logica JS (non della UX): step indicator ora usa `data-state="active"/"done"` invece di classi multiple; drop zone usa `data-dragging="true"` invece di `.drag-over`; modal invito usa l'attributo `hidden` invece di `.open` (stesso pattern già adottato in `home.html` nel task 25). `frontend/admin/css/admin.css` ridotto da 419 a 196 righe — rimosse le regole duplicate (reset, root vars, button, panel, table, messages/empty, wizard steps indicator, drop-zone, modal, badge, h4) ora tutte fornite da `frontend/shared/design-system.css`/tokens; mantenute solo le regole page-shell (login, topbar, aside, content, page routing) e quelle non coperte dal design system (form/label/input/select bare-tag, perché i form dell'admin non sono stati convertiti al componente `ds-input` — restano `<label>`/`<input>` semplici). Verificato con Playwright headless (login → dashboard → tutti e 4 gli step del wizard → modal invito allenatore) su dati POC: zero errori JS/console, visivamente conforme al design system. Suite di test: 172 passed, stessi 3 fallimenti pre-esistenti in `test_scoring.py` (non correlati, già documentati nel task 25) + 3 errori pre-esistenti in `test_fbref_scraper.py` per `pytest-mock` mancante nell'ambiente (non correlato, non toccato da questo task). **Follow-up 2026-07-09** (branch `claude/task-26-ds-factories-followup`): dopo che l'utente ha aggiunto `prompts/frontend/26-restyle-admin-spa.md` a posteriori (commit diretto su `main`), verificati gli acceptance criteria e trovati 2 gap reali non coperti dalla PR #81: (1) il prompt richiede esplicitamente l'uso delle factory vanilla JS del task 24 (`WizardSteps`, `Panel`, `DropZone`, `Table`, `Modal`, `Button`, `Badge`), non solo le classi CSS — a differenza delle pagine pubbliche SSR (task 25), l'Admin SPA è puro client-side, quindi nessun vincolo di hydration lo impediva; (2) sidebar ancora nascosta con `display:none` sotto 640px, nessun hamburger. Risolto: step indicator ricostruito con `DS.WizardSteps()` (slot `#wizardStepsSlot`, stato "done" tracciato in `stepDone[]` e riapplicato ad ogni render perché il factory non lo gestisce nativamente); i 2 upload (listone/formazioni) usano `DS.DropZone()`; tutte le tabelle dinamiche (dashboard leghe, manager, assegnazione giocatori, stato allenatori, pool assegnati, mapping completo, punteggi giornata, Gran Premi) ricostruite con `DS.Table()` + `DS.Badge()` per i ruoli + `DS.Button()` per le azioni in cella; pannelli di riepilogo dinamici (importazione, mapping, sorteggio, punteggi, mapping per-manager) con `DS.Panel()`; modal "Link invito" ricostruito con `DS.Modal()` al posto del markup statico + script custom. Scope escluso deliberatamente: i pannelli statici del wizard (con gli help-box/file-spec aggiunti dal task 28) e i bottoni statici non sono stati ricostruiti via factory — solo le classi CSS esistenti, per non destabilizzare quel lavoro già mergiato; nessuna criteria esplicita lo richiedeva per quelle parti. Hamburger mobile: aggiunto `#asideToggle` in topbar + `#adminAside` come drawer off-canvas sotto 640px (stesso pattern di `.nav-toggle` introdotto dalla PR #84 per le pagine pubbliche, adattato a sidebar verticale invece di nav orizzontale). Rimossa `setupDropZone()` (helper generico ormai morto, sostituito dal factory). Verificato con Playwright headless (login → dashboard → 4 step wizard → modal invito → hamburger a viewport 375px) su dati POC: zero errori JS/console (solo rumore di rete atteso: font Google bloccati dal sandbox, 401 su `/auth/me` pre-login). Suite di test: 172 passed, stessi fallimenti pre-esistenti non correlati.
- [x] **27** — Restyle Coach SPA → nessun prompt scritto in `prompts/` (gap segnalato e chiarito con l'utente a inizio sessione con 2 domande: confermato approccio CSS-only come 25/26 invece di componentizzazione JS completa; confermato di includere anche `punteggi.html`, non citato nelle note della sessione precedente ma parte della Coach SPA). Branch: `claude/prossimo-task-6xloxn` (non `task/27-restyle-coach-spa` come da convenzione — imposto dall'harness). Modifiche: le 4 pagine (`login.html`, `index.html`, `rosa.html`, `punteggi.html`) caricano `/shared/design-system.css` prima di `css/coach.css`; classi custom sostituite sia nel markup statico sia nei template string JS: `.tabs`/`.tab-btn`/`.tab-panel`→`ds-tabs__nav`/`ds-tabs__tab` (aria-selected) + attributo `hidden` sui pannelli (stesso pattern di `home.html` task 25), `.login-card`→aggiunta `ds-panel ds-panel--default`, `button`/`.secondary`/`.danger`→`ds-button`+varianti, `.msg`→`ds-message`, `.panel`→`ds-panel ds-panel--default`/`ds-panel--accent` (banner Gran Premio vinto), `.badge`/`.badge-green`/`.badge-red`→`ds-badge`+varianti, `.empty`→`ds-empty-state`+`ds-empty-state__message`, tabella punteggi (generata via `innerHTML` in `punteggi.html`, non aveva classe prima)→`ds-table-wrap`+`ds-table`+`ds-table__row`, progress bar del lock-bar in `rosa.html`→`ds-progress-bar__track`/`__fill` al posto di `.progress-bar-wrap`/`.progress-bar-fill` custom. `frontend/coach/css/coach.css` ridotto da 332 a 159 righe — rimossi `:root`, reset globale, scanline CRT, `.badge*`, `.tabs`/`.tab-btn*`/`.tab-panel*`, `button`/`.secondary`/`.danger`, `.msg*`, `.panel*`, `.progress-bar-wrap`/`.progress-bar-fill`, `.empty` (ora tutti forniti da `frontend/shared/design-system.css`/tokens); mantenute solo le regole page-shell (topbar, content, login-wrap/login-card dimensioni) e quelle non coperte dal design system (form/label/input/select bare-tag — stessa scelta di admin/task 26 — e le classi dominio-specifiche `role-section`/`role-header`/`player-row`/`player-card`/`arrow`/`assign-select`/`league-card`/`lock-bar`, non parte del design system condiviso). Verificato end-to-end con Playwright headless su dati POC (`database/seed_poc.py` + invito/registrazione coach + assegnazione pool nostalgia via `/admin/league/{id}/mapping/assign-pools`): login → tab registrati → le mie leghe → rosa (con pool assegnato, panel/badge/progress-bar/player-row visibili) → punteggi (tabella ds-table) — zero errori JS/console (gli unici eventi in console sono `ERR_CONNECTION_RESET` sul fetch dei Google Fonts, dovuto al sandbox di rete, non correlato). Suite di test: 172 passed, stessi 3 fallimenti pre-esistenti in `test_scoring.py` + 3 errori pre-esistenti in `test_fbref_scraper.py` (pytest-mock mancante), entrambi non correlati e già documentati nei task 25/26. **Follow-up 2026-07-09** (branch `claude/task-27-ds-factories-followup`): dopo che l'utente ha aggiunto `prompts/frontend/27-restyle-coach-spa.md` a posteriori (commit diretto su `main`), verificati gli acceptance criteria e trovati 2 gap reali non coperti dalla PR #82 (stesso schema dei follow-up 25/26 e 26/28): (1) navigazione mobile con hamburger assente su tutte e 4 le pagine coach; (2) contenuto dinamico (badge di stato, bottoni delle league-card, tabella punteggi, progress bar del lock-bar) scritto come classi CSS `ds-*` via `innerHTML`/template string invece delle factory JS `Badge`/`Button`/`Table`/`ProgressBar`/`EmptyState` richieste esplicitamente dal prompt — differenza non solo stilistica: `DS.ProgressBar()` aggiunge `role="progressbar"`+`aria-valuenow/min/max`, gap di accessibilità reale rispetto al markup scritto a mano (verificato leggendo `design-system.js`). Il Modal per "Conferma e Blocca" (via `DS.confirmDialog`) e l'HelpBox su "Associa l'alter ego" erano invece già stati aggiunti dal task 28, che assumeva erroneamente fossero "già presenti dal task 27" — nessun ulteriore intervento necessario lì. Risolto: hamburger mobile aggiunto a `index.html`/`rosa.html`/`punteggi.html` (non a `login.html`, che non ha topbar) con lo stesso pattern `.nav-toggle`+`.topbar-actions.open` delle PR #84/#85, adattato dal `<nav>`/`<aside>` a un div `.topbar-actions` che raccoglie le azioni a destra della topbar (badge di stato, link Punteggi/Rosa, bottone Esci); `index.html` ricostruisce le league-card con `DS.Badge()`+`DS.Button()` invece di template string (rimossa la funzione `esc()` diventata inutilizzata); `rosa.html` usa `DS.Badge()` per lo statusBadge e `DS.ProgressBar()` per il lock-bar (mantenuto il testo `progressText` esterno invariato, sostituito solo il track/fill); `punteggi.html` ricostruisce la tabella giornate con `DS.Table()` (colonne con `pixel`/`color` per riprodurre esattamente la colorazione precedente) e l'empty state con `DS.EmptyState()`. Scope escluso deliberatamente, per coerenza con la stessa scelta fatta nei follow-up di admin/pagine pubbliche: `showMsg()` (toast messaggi) non convertito a `DS.Message()` — l'admin follow-up (PR #85) non l'ha fatto neanche lì, lasciato invariato per non introdurre un'inconsistenza tra le due SPA; tabs statiche di login/registrazione e bottoni statici nei form non convertiti (matcha `home.html` del task 25, mai reso componente neanche lì). Verificato con Playwright headless a 2 viewport (1280px desktop, 375px mobile) su dati POC (`database/seed_poc.py` + invito/registrazione coach + assegnazione pool nostalgia): login → le mie leghe (hamburger aperto/chiuso) → rosa (hamburger, badge, help-box, progress bar con `role="progressbar"` verificato via query selector) → punteggi (hamburger, tabella `DS.Table()`) — zero errori JS/console. Suite di test: 172 passed, stessi fallimenti pre-esistenti non correlati.
- [x] **28** — Pattern esplicativi (help-box, file-spec, confirm-dialog) → nessun prompt scritto in `prompts/frontend/` (gap segnalato e chiarito con l'utente a inizio sessione con 3 domande: file-spec limitato alle 2 upload già in UI — rose/formazioni Excel, escluso i 3 flussi CLI-only di `docs/design-system-brief.md` §6b; confirm-dialog limitato alle 3 conferme esplicite della spec §6c — Apri buste, Chiudi periodo associazioni, Conferma e Blocca; niente template Excel scaricabili — Rose_esempio.xlsx/Formazioni_esempio.xlsx restano fuori scope, nessuna nuova funzione oltre al restyling). Branch: `claude/prossimo-task-h8zea3` (imposto dall'harness). Aggiunti 2 nuovi componenti alla libreria design system (`frontend/shared/design-system.js`): `FileSpec({format, structure, note})` — box con badge formato file + descrizione struttura attesa, stile analogo a HelpBox ma bordo tratteggiato; `confirmDialog({title, message, consequence, confirmLabel, cancelLabel, danger})` — wrapper Promise-based sopra `Modal`+`Button` già esistenti (risolve `true`/`false`), pensato per sostituire `confirm()` nativo con pattern `if (!await DS.confirmDialog({...})) return;`. CSS in `frontend/shared/design-system.css`: `.ds-file-spec*` e `.ds-confirm-dialog*` (variante `--danger` con bordo/titolo rossi). Demo aggiornato in `frontend/shared/demo.html`. **Prima volta che `design-system.js` viene effettivamente caricato ed eseguito in produzione** (task 24-27 avevano riusato solo le classi CSS, mai le factory JS) — aggiunto `<script src="/shared/design-system.js"></script>` in `frontend/admin/index.html` (prima di `js/api.js`) e in `frontend/coach/rosa.html` (in `<head>`), più `const DS = window.FantaNostalgiaDS;` nei rispettivi script inline. Admin: Step 1 (Carica listone) e Step 4 (Carica formazioni) ora hanno HelpBox + FileSpec con la struttura Excel attesa (da `CLAUDE_MEMORY.md`); i paragrafi statici già esistenti in Step 2 (assegna pool, inviti, chiudi periodo) e Step 3 (apertura buste) sono stati convertiti da `<p style="...">` a `ds-help-box`/`ds-help-box--warn`; le due righe rosse "Operazione irreversibile" separate sono state rimosse a favore del testo `consequence` mostrato nel confirm-dialog al momento del click. I 2 `confirm()` nativi in admin (`closeAssociationsBtn`, `revealBtn`) sostituiti con `DS.confirmDialog(...)`. Coach `rosa.html`: aggiunto nuovo HelpBox statico "Associa l'alter ego" (prima non esisteva alcuna spiegazione del meccanismo di associazione storico→attuale), mostrato/nascosto insieme a `#main`; il `confirm()` nativo di `lockBtn` sostituito con `DS.confirmDialog(...)`. Verificato end-to-end con Playwright headless su dati POC (`database/seed_poc.py` + pool assignment + invito/registrazione coach via API dirette): tutti i 4 step admin (help-box/file-spec visibili, confirm-dialog apre/chiude correttamente su Annulla senza nodi DOM residui), flusso coach completo incluso click reale su "Conferma e Blocca" → dialog → "Blocca" → lock riuscito con badge "Bloccato". Zero errori console/JS (unico evento è `ERR_CONNECTION_RESET` su Google Fonts, dovuto al sandbox di rete, non correlato — già documentato nei task precedenti). Suite di test: 172 passed, stessi 3 fallimenti pre-esistenti in `test_scoring.py` + 3 errori pre-esistenti in `test_fbref_scraper.py` (non correlati, già documentati nei task 25-27).
- [x] **28-followup** — Allineamento al prompt `prompts/frontend/28-pattern-esplicativi.md` (aggiunto a posteriori dopo il merge della PR #83, riletto e confrontato acceptance-criteria-vs-implementazione su richiesta esplicita di Simone) → branch `claude/task-28-pattern-esplicativi-followup`. Gap trovati e chiusi (3): **(1)** i due `FileSpec` (Step 1 listone, Step 4 formazioni) non avevano link a un file di esempio scaricabile — creati `backend/static/examples/Rose_esempio.xlsx` e `Formazioni_esempio.xlsx` (validati con gli stessi parser di produzione `_parse_rose_rows`/`_parse_formazioni_rows`, zero warning), aggiunto parametro `link`/`linkText` a `FileSpec` in `design-system.js`/`.css` e collegati nei 2 punti. **(2)** Mancava un HelpBox sul flusso di import dati storici via CSV (`POST /admin/historic/import`, resta CLI-only) — aggiunto un blocco `<details>` collassato "Dati storici (avanzato)" nella Dashboard admin con HelpBox esplicativo. **(3)** 5 `EmptyState` in admin passavano solo `message` senza `action`/`onAction` nonostante il componente li supporti già dal task 24 — aggiunte CTA dove sensato: "Nessuna lega" → apre form nuova lega; "Nessun manager" (Step 1, 2 punti) → apre/focus il form "+ Aggiungi manager"; "Nessun manager" (Step 2, coach status) → torna a Step 1; "Nessun Gran Premio" → focus sul form di creazione. Empty state nelle pagine pubbliche SSR e in coach (`Nessuna lega trovata`, `Nessuna giornata disputata`) lasciati senza CTA: nessuna azione in-app sensata per un visitatore anonimo o un coach in attesa. Verificato con Playwright headless: download dei 2 esempi (200 OK), apertura del box dati storici, tutte le CTA testate aprono/navigano correttamente. Test: 172 passed, stessi 3 fallimenti pre-esistenti in `test_scoring.py` (non correlati).
- [x] **29** — Gestione multi-lega in Admin → `prompts/frontend/29-multi-lega-admin.md` (branch: `claude/prossimo-task-oga3tp`, imposto dall'harness). Verifica preliminare: `league.py` esponeva già `GET /league` (lista), `GET /league/{id}`, `POST/PUT/DELETE /admin/league`; la Dashboard Admin (`frontend/admin/index.html`) aveva già, fin dal task 10, l'elenco leghe con bottoni Gestisci/Modifica + link "+ Nuova lega" + wizard scoped su `currentLeagueId` — confermato con `git log --follow` che risale a `feat(task-10)`. I primi 3 acceptance criteria del prompt erano quindi già soddisfatti. **Blocco reale trovato:** il prompt richiede un form di creazione lega con "nome, stagione storica, **max manager**, **piattaforma**" — nessuno dei due campi esisteva in `database/schema.sql` né altrove nel progetto (verificato con grep su tutto `docs/`). Il prompt stesso istruisce di fermarsi e chiedere approvazione prima di una migrazione: fermato, chiesto a Simone, **approvato** "aggiungi entrambe le colonne". Modifiche: `database/schema.sql` — aggiunte `max_manager INTEGER` e `platform TEXT` alla tabella `league`; `backend/api/db.py` — `ALTER TABLE league ADD COLUMN` in fallback per i DB già esistenti (stesso pattern già in uso per `associations_closed`); `backend/api/routers/league.py` — `LeagueCreate`/`LeagueUpdate` con i 2 campi opzionali (validazione `max_manager >= 1` se presente), `create_league`/`update_league` li persistono, `create_manager` ora rifiuta con 422 "Numero massimo di manager raggiunto" se il limite è già raggiunto; `GET /league` ampliato da una `SELECT` a 4 colonne a `SELECT *` (fix collaterale: escludeva già `budget`, quindi la dashboard mostrava sempre "—" per il budget — bug preesistente, corretto di riflesso perché serviva comunque includere `platform`). Frontend: form "Nuova/Modifica Lega" ricostruito con `DS.Input`/`DS.Button`/`DS.Panel` (era l'unico form nell'app rimasto con `<input>` semplici, richiesto esplicitamente dal prompt) con i 2 campi nuovi; dashboard — colonna "Piattaforma" aggiunta alla tabella leghe; pannello "Manager della lega" nel wizard mostra "(N/max)" quando `max_manager` è impostato. Test: 5 nuovi in `backend/tests/test_league.py` (creazione con/senza i 2 campi, validazione max_manager < 1, isolamento manager tra 2 leghe, blocco al raggiungimento del limite) — 177 passed (172 + 5), stessi 3 fallimenti pre-esistenti in `test_scoring.py` + 3 errori pre-esistenti in `test_fbref_scraper.py` (non correlati, già documentati). Verificato con Playwright headless: creazione lega con tutti i campi, colonna Piattaforma visibile, hint manager 0/2→1/2→2/2, 3° manager rifiutato con messaggio corretto, precompilazione corretta in "Modifica", form visibile a 375px — zero errori console/JS.
- [x] **30** — Elevazione coach → admin → `prompts/backend/30-elevazione-coach-admin.md` (branch: `claude/sotto-a-chi-tocca-9onli8`, imposto dall'harness). **`database/schema.sql` NON toccato** — scoperto e confermato con Simone prima di scrivere codice che il file non è più la fonte di verità completa: le tabelle post-POC (`user`, `league_invite`, `manager_nostalgia_pool`, `gran_premio`, `h2h_match`) vivono solo in `backend/api/db.py::init_db()` (blocco `CREATE TABLE IF NOT EXISTS` + `ALTER TABLE ADD COLUMN` in fallback per i DB già deployati), mai state aggiunte al file protetto — a differenza del task 29 dove la modifica riguardava `league`, tabella presente in `schema.sql` fin dalla POC. Stesso pattern riusato qui: aggiunta colonna `user.is_admin INTEGER DEFAULT 0` (sia nel `CREATE TABLE IF NOT EXISTS user` per i DB nuovi, sia in `ALTER TABLE` fallback per quelli esistenti — la sola ALTER non basta perché su un DB davvero vergine la tabella `user` non esiste ancora quando quel blocco gira, ordine di esecuzione verificato) e nuova tabella `admin_elevation_request` (id, user_id, status pending/approved/rejected, requested_at, resolved_at, resolved_by). **Scoperta architetturale chiave** (root del "Doubt-Driven Review" del prompt): l'admin oggi NON è una riga della tabella `user` — è un'identità globale unica da env var (`ADMIN_USERNAME`/`ADMIN_PASSWORD`, cookie `session`), del tutto separata dal login coach (tabella `user`, cookie `user_session`). Risolto senza introdurre un sistema di ruoli: `get_current_admin` in `backend/api/routers/auth.py` ora, se assente il cookie `session`, accetta anche `user_session` con `user.is_admin=1` — un'unica funzione già usata da `Depends()` in tutti i router admin esistenti (league.py, players.py, lineups.py, granpremio.py, ecc.), quindi **nessun router modificato singolarmente**, zero duplicazione della logica di sessione. L'elevazione è **globale** (come l'admin attuale, che gestisce già tutte le leghe), non scoped per lega — coerente col modello esistente, evita un RBAC per-lega non richiesto. Endpoint nuovi (tutti in `auth.py`, dato che è l'intersezione tra i due sistemi di auth già esistenti — nessun nuovo router creato): `POST /auth/user/elevation-request` (coach, blocca richieste duplicate pendenti — questa è la mitigazione anti-spam usata al posto di un rate-limiter dedicato, che non esiste altrove nel codebase), `GET /auth/admin/elevation-requests`, `POST /auth/admin/elevation-requests/{id}/approve`, `POST /auth/admin/elevation-requests/{id}/reject` (tutti admin-only, tracciano `resolved_by`/`resolved_at` per audit minimo). `GET /auth/user/me` esteso con `is_admin`/`elevation_status`. Fix collaterale: `doLogout()` in `frontend/admin/js/auth.js` ora cancella anche il cookie `user_session` — senza, un coach elevato che clicca "Esci" nell'Admin SPA restava autenticato (bug reale scoperto verificando il logout con Playwright). Frontend: `frontend/coach/index.html` — nuovo pannello con `DS.HelpBox` + `DS.Button`/`DS.Badge` (richiedi / in attesa / già admin con link a `/admin`); `frontend/admin/index.html` — pannello "Richieste di elevazione admin" in dashboard con `DS.Table` + `DS.confirmDialog` per approva/rifiuta (nuovo file `frontend/admin/js/elevation.js` per le chiamate API, stesso pattern di `league.js`/`mapping.js`). Test: `backend/tests/test_elevation.py` (5 nuovi: richiesta richiede auth, richiesta→approvazione→verifica permessi via endpoint admin-only, richiesta→rifiuto→permessi invariati, richiesta duplicata rifiutata, doppia risoluzione della stessa richiesta rifiutata) — 184 passed (179 + 5), stessi 3 fallimenti pre-esistenti in `test_scoring.py` + 3 errori pre-esistenti in `test_fbref_scraper.py` (non correlati, già documentati nei task precedenti). Verificato end-to-end con Playwright headless (admin crea lega/manager/invito → coach si registra e richiede elevazione → admin approva da dashboard con conferma esplicita → coach ricarica e vede "Sei amministratore" → coach visita `/admin/` direttamente con le proprie credenziali coach, senza aver mai fatto login admin, e vi accede regolarmente → logout invalida entrambi i cookie): zero errori JS reali (solo `ERR_CONNECTION_RESET` su Google Fonts, sandbox di rete, non correlato, già documentato nei task precedenti).
- [x] **31** — Pagina pubblica "Come Funziona" standalone → `prompts/frontend/31-pagina-come-funziona.md` (branch: `claude/dove-simao-adesso-inmhjg`, imposto dall'harness). Nuova route `GET /come-funziona` (`backend/api/routers/views.py`) con template dedicato `backend/templates/come-funziona.html` (estende `base.html`, stesso stile delle altre pagine pubbliche SSR — classi `ds-*` esistenti, nessuna dipendenza JS aggiuntiva). Contenuto: 3 step (asta invariata, apertura buste/mapping alter ego, sorteggio giornata + formula punteggio `P = V + B − M` con legenda e un esempio numerico di scontro diretto — stesso esempio del mockup `ComeFunziona.dc.html` ma con la conversione punti→gol realmente implementata in `standings.py::_score_to_goals`, non inventata), tabella "Regole principali" (voti reali/sintetici, bonus/malus, giocatore senza alter ego, copertura minima rosa, duplicati, reveal, sorteggio — solo regole già confermate), FAQ ad accordion (3 domande richieste dal prompt: piattaforma per l'asta, stagioni storiche disponibili, chi può creare una lega — risposta onesta sullo stato attuale, senza descrivere il flusso di elevazione admin del task 30 non ancora completato). Deliberatamente omesse wildcard storica, mercato di riparazione, tie-break su gol storici (spike 32/34/35, non risolti) — verificato che il copy del mockup che le menzionava non è stato riportato. Nuovo componente **Accordion** aggiunto a `frontend/shared/design-system.js`/`.css` (factory `DS.Accordion({items})`, pattern bottone+pannello con `aria-expanded`/`aria-controls`, coerente con gli altri componenti del task 24) — la pagina SSR però implementa l'accordion con le sole classi `ds-accordion*` + un piccolo script inline (stesso pattern già usato per i tab in `home.html`/`statistiche.html`: le pagine pubbliche SSR non caricano `design-system.js`), il factory resta disponibile per riuso futuro lato client (admin/coach). `home.html`: la sezione "Come funziona" è stata ridotta a un breve sommario con link `/come-funziona` (rimossi i 4 step dettagliati e il CSS ora morto `.step-list`/`.step`/`.step-num`/`.step-body`). Test: `backend/tests/test_come_funziona.py` (2 nuovi: la pagina carica con le sezioni #regole/#faq, home linka la nuova pagina) — 179 passed (177 + 2), stessi 3 fallimenti pre-esistenti in `test_scoring.py` + 3 errori pre-esistenti in `test_fbref_scraper.py` (non correlati, già documentati nei task precedenti). Verificato con Playwright headless a 2 viewport (1200px, 375px): navigazione anchor (#regole, #faq), toggle accordion (aria-expanded + pannello visibile/nascosto), link home→come-funziona — zero errori console/JS (solo `ERR_CONNECTION_RESET` su Google Fonts, sandbox di rete, non correlato).
- ⏳ **32** — Wildcard storica — SPIKE PENDING → `docs/spike-32.md`
- ⏳ **33** — Mercato di riparazione (gennaio) — SPIKE PENDING → `docs/spike-33.md`
- ⏳ **34** — Tie-break su gol storici — SPIKE PENDING → `docs/spike-34.md`
- ⏳ **35** — Sorteggio giornata con override manuale — SPIKE PENDING → `docs/spike-35.md`
---
 
## Prossima sessione — inizia da qui (per questa epica)

Task 24, 25, 26, 27, 28, 29, 30 e 31 tutti completati (24-28 con i rispettivi follow-up di allineamento: 25→PR #84, 26→PR #85, 27→PR #86, 28→PR #87). Task 29 (PR #88) e 31 (PR #89) mergiati in `main`. Task 30 (Elevazione coach→admin) completato in questa sessione, vedi dettagli sopra — PR non ancora aperta (solo push del branch). **Pattern confermato per l'epica: ogni volta che viene aggiunto un prompt a posteriori per un task già completato, va riletto e confrontato acceptance-criteria-vs-implementazione (è già successo per 25, 26, 27 e 28) — non dare per scontata la compliance solo perché il task risulta `[x]`.** Nessun task rimasto da avviare in questa epica a parte le spike 32-35 (bloccate). Prossimo passo possibile: verificare se PR #90 (task 30) viene aperta/mergiata, e se in futuro arriva un prompt a posteriori anche per 29/30/31 rileggerlo con lo stesso pattern.

Ordine di esecuzione: 24→31 tutti fatti. Le task 32-35 restano bloccate finché non vengono risolte con `/cdp:spike-integrate` dopo aver riempito i rispettivi `docs/spike-NN.md`.
 
## Decisioni prese epica 4
 
- Modalità: **FEATURE-ADDITION** su repo esistente `fantanostalgia`.
- Scartate esplicitamente (non implementare): dashboard KPI admin, sezione manager standalone, catalogo stagioni esplorabile, coach dashboard doppia classifica, rimozione associazione alter ego pre-lock, import listone via API piattaforme esterne (vedi `project-spec.md` sez. 13 per l'elenco completo).
- Il "link invito coach con token" **esiste già** nell'app — task 26 lo restyla soltanto, non lo ricostruisce.
- Vincolo architetturale: nessuna dipendenza React nel prodotto finale — il design system va portato in vanilla JS (task 24).
- Numerazione continua da dove si era fermata l'epica 3 (ultimo task completato: 23 — Gran Premi di giornata).
 
---

## Formato Excel reale (da Rose_erculotuo.xlsx e Formazioni_erculotuo_36_giornata.xlsx)

### Rose (listone con rose):
- Sheet unico "TutteLeRose", 9 colonne (A-I)
- Due squadre affiancate: left (A-D), separator col E vuota, right (F-I)
- Colonne: Ruolo | Calciatore | Squadra (reale, es. 'Juve') | Costo (prezzo asta)
- Header squadra: col A = nome squadra fanta, col F = nome squadra fanta (right)
- Fine blocco: 'Crediti Residui: X'
- 10 squadre × ~25 giocatori = 250 giocatori totali
- Parser: `_parse_rose_rows` in `backend/api/routers/players.py`
- Auto-assegna giocatori ai manager per `team_name` (case-insensitive)

### Formazioni (risultati giornata):
- Sheet per giornata, es. "Formazioni 36 giornata", 11 colonne (A-K)
- Due match affiancati: left (A-E), col F = risultato (es. '0-1'), right (G-K)
- Header match: col A = squadra sinistra, col F = 'X-Y', col G = squadra destra (UPPERCASE)
- Colonne per ogni squadra: Ruolo | Calciatore | vuota | Voto_no_bonus | Voto_con_bonus
- 'Panchina' separa titolari da panchina (is_starter = 0)
- 'TOTALE: XX,YY' marca fine della squadra
- '-' = giocatore senza voto (sv o non entrato)
- Parser: `_parse_formazioni_rows` in `backend/api/routers/lineups.py`
- Lookup manager per `team_name` (case-insensitive) oltre che per `name`

---

## Coach API

- Router: `backend/api/routers/coach.py`
- Frontend: `frontend/coach/login.html`, `frontend/coach/index.html`, `frontend/coach/rosa.html`
- Endpoint chiave: `GET /coach/league/{id}/rosa` — mostra pool nostalgia del manager con associazioni storico→attuale
- Associazione manuale disponibile da admin panel

---

## Blockers

- Cloudflare su fbref.com può bloccare richieste da datacenter IP (Cloud Run). Usare lo scraper in locale oppure usare `calcio-seriea.net` che non è soggetto a questo blocco.

## PR completate

- [#1](https://github.com/simonediroma/fantanostalgia/pull/1) — task/00-seed-poc ✓ mergiata
- [#2](https://github.com/simonediroma/fantanostalgia/pull/2) — task/01-bootstrap ✓ mergiata
- [#3](https://github.com/simonediroma/fantanostalgia/pull/3) — task/02-import-listone ✓ mergiata
- [#7](https://github.com/simonediroma/fantanostalgia/pull/7) — task/03-mapping-alter-ego (`claude/gallant-archimedes-lwbn3i`) ✓ mergiata
- [#8](https://github.com/simonediroma/fantanostalgia/pull/8) — task/04-apertura-buste (`claude/kind-volta-oqq3rf`) ✓ mergiata
- [#9](https://github.com/simonediroma/fantanostalgia/pull/9) — task/05-import-formazioni (`claude/tender-allen-uyaspn`) ✓ mergiata
- [#10](https://github.com/simonediroma/fantanostalgia/pull/10) — task/06-sorteggio-giornata (`claude/prossimo-task-gztmd8`) ✓ mergiata
- [#11](https://github.com/simonediroma/fantanostalgia/pull/11) — task/07-calcolo-punteggi (`claude/prlssimo-task-fzkcwv`) ✓ mergiata
- [#12](https://github.com/simonediroma/fantanostalgia/pull/12) — task/08-api-classifica (`claude/memory-file-review-fecztn`) ✓ mergiata
- [#13](https://github.com/simonediroma/fantanostalgia/pull/13) — task/09-layout-base (`claude/task-09-nsvx6j`) ✓ mergiata
- [#16](https://github.com/simonediroma/fantanostalgia/pull/16) — task/11-pagine-pubbliche (`claude/prossimo-task-lo5jxx`) ✓ mergiata
- [#17](https://github.com/simonediroma/fantanostalgia/pull/17) — prompt task 16 aggiunto (`claude/8bit-aesthetic-redesign-459mxp`) ✓ mergiata
- [#31](https://github.com/simonediroma/fantanostalgia/pull/31) — scraper fbref + RatingWeights + CSV export/import + understat (`claude/data-scraping-approach-jksb5l`) ✓ mergiata
- [#32](https://github.com/simonediroma/fantanostalgia/pull/32) — requirements-scraper.txt (`task/requirements-scraper`) ✓ mergiata
- [#33](https://github.com/simonediroma/fantanostalgia/pull/33) — scraper fbref con Playwright per bypass Cloudflare (`task/fbref-playwright`) ✓ mergiata
- [#34](https://github.com/simonediroma/fantanostalgia/pull/34) — Web Scraper Chrome sitemap + convert_webscraper.py (`task/fbref-webscraper-sitemap`) ✓ mergiata
- [#35](https://github.com/simonediroma/fantanostalgia/pull/35) — Coach API: auth, nostalgia pool, associazione manuale (`claude/nostalgia-player-association-u5semr`) ✓ mergiata
- [#36](https://github.com/simonediroma/fantanostalgia/pull/36) — Fix CI/CD: Artifact Registry (`task/fix-artifact-registry`) ✓ mergiata
- [#38](https://github.com/simonediroma/fantanostalgia/pull/38) — Guida seasons.html per multi-season scraping (`task/seasons-html-links`) ✓ mergiata
- [#39](https://github.com/simonediroma/fantanostalgia/pull/39) — Scraper calcio-seriea.net con ruoli reali (`claude/upbeat-fermat-98un5f`) ✓ mergiata
- [#80](https://github.com/simonediroma/fantanostalgia/pull/80) — task/25-restyle pagine pubbliche + Hall of Fame (`claude/prossimo-task-1wzl48`) ✓ mergiata
- [#88](https://github.com/simonediroma/fantanostalgia/pull/88) — task/29-gestione multi-lega in Admin (`claude/prossimo-task-oga3tp`) — aperta
- [#90](https://github.com/simonediroma/fantanostalgia/pull/90) — fix Dockerfile.backend, `frontend/shared/` mancante in produzione (`claude/homepage-display-issue-k5nby2`) ✓ mergiata
- branch `claude/ranking-calendar-team-display-7l5l18` — priorità nome nostalgia + icone bonus/malus nel tab Calendario — pushato, PR non ancora aperta
- [#96](https://github.com/simonediroma/fantanostalgia/pull/96) — notifiche email ai manager via Resend, prima versione con invio sincrono via `BackgroundTasks` (`claude/manager-email-notifications-ftili7`) ✓ mergiata
- [#97](https://github.com/simonediroma/fantanostalgia/pull/97) — refactor a coda email su DB + scodamento via GitHub Actions cron, sullo stesso branch ricostruito su `main` dopo il merge di PR #96 (`claude/manager-email-notifications-ftili7`) ✓ mergiata
